name: zenpay_example_backend
description: Minimal reference merchant backend for the combined ZenPay example (sessions, callbacks, return, health).
publish_to: none
version: 0.1.0-dev.1

resolution: workspace

environment:
  sdk: ^3.12.0

dependencies:
  dotenv: ^4.2.0
  googleapis_auth: ^2.0.0
  googleapis_beta: ^9.0.0
  hashlib: ^2.4.2
  logging: ^1.3.0
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  zenpay_dart: ^0.1.0

dev_dependencies:
  http: ^1.6.0
  lints: ^6.1.0
  test: ^1.31.0
