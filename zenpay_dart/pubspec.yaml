name: zenpay_dart
description: Pure-Dart ZenPay Hosted Checkout Plugin (zp-hcp) shared and server-side API parity — cryptography, callback verification, and launch-URL construction.
version: 0.1.1
homepage: https://www.zenithpayments.com.au
repository: https://github.com/ianmenethil/zp-flutter/tree/master/zenpay_dart

resolution: workspace

environment:
  sdk: ^3.13.0

dependencies:
  pointycastle: ^4.0.0

dev_dependencies:
  coverage: ^1.15.1
  lints: ^6.1.0
  test: ^1.31.0
